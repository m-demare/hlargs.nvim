(parameter_list
  (parameter_declaration
    (identifier) @argname))
(parameter_list
  (parameter_declaration
    (_
        (identifier) @argname)))
(parameter_list
  (parameter_declaration
    (_
      (_
        (identifier) @argname))))
(parameter_list
  (parameter_declaration
    (_
      (_
        (_
          (identifier) @argname)))))
; There has to be a better way to do this

